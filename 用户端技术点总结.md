# 用户端开发技术点总结

## 1. 文档范围

本文档基于《需求矩阵》中“用户端部分”的需求项进行总结，重点说明当前 `ChargingUserClient` 用户端项目中，每个需求对应采用的 Qt/C++ 技术、核心类、核心函数和实现方式。

当前用户端具备两套数据通路：

- 服务端模式：检测到服务端 `127.0.0.1:8888` 可用后，通过 `QTcpSocket` 与 `EVChargingServer` 通信。
- 本地演示模式：服务端不可用时，使用本地 SQLite 数据库完成注册、登录、钱包、电站、电桩和订单演示。

相关工程路径：

- 用户端项目：`ChargingUserClient`
- 服务端项目：`EVChargingServer`
- 一键验证脚本：`validate_all.ps1`
- Socket 冒烟测试：`socket_smoke.cpp`

## 2. 用户端整体技术路线

| 技术点 | 使用位置 | 作用 |
| --- | --- | --- |
| Qt Widgets | `mainwindow.ui`、`mainwindow.cpp` | 构建登录、注册、钱包、电站、充电、个人资料等桌面界面 |
| Qt Designer UI 文件 | `mainwindow.ui` | 使用 `.ui` 设计界面，程序启动时由 `uic` 生成界面类 |
| 信号与槽 | `MainWindow::setupConnections()` | 连接按钮点击、页签切换、表格选择、充电进度等事件 |
| QStackedWidget | 登录页、注册页、找回密码页、主应用页 | 实现多页面切换 |
| QTabWidget | 主应用内部功能页 | 实现“附近电站 / 充电 / 钱包 / 个人资料”等功能页 |
| QSS 样式表 | `resources/styles/userclient.qss` | 统一字体、按钮、表格、卡片、进度条、登录背景等视觉样式 |
| Qt 资源系统 | `resources.qrc` | 管理图标、登录背景图、默认头像和 QSS |
| SQLite | `DatabaseManager` | 本地演示模式下保存用户、钱包、电站、电桩、订单、流水 |
| TCP Socket | `ServerApiClient`、`ClientProtocol` | 服务端模式下对接 `EVChargingServer` |
| 自定义协议 | `clientprotocol.h/.cpp` | 使用协议头 + 命令号 + `QVariantMap` 实现客户端/服务端请求响应 |
| QThread + QTimer | `ChargingWorker` | 模拟充电过程，避免阻塞主界面 |
| 事件过滤器 + 绘制事件 | `MouseTrailWidget` | 实现鼠标拖尾视觉效果 |
| 绘制事件 | `TechPage` | 绘制登录页科技感背景线条 |

## 3. 需求矩阵与实现技术对应

### 3.1 需求 #8：注册功能

需求摘要：用户输入手机号、验证码、密码、昵称，完成账号注册；需要校验手机号、验证码、密码和昵称格式。

实现技术：

- 使用 `QLineEdit` 获取手机号、验证码、密码、昵称。
- 使用 `QRegularExpression` 完成格式校验。
- 使用 `QHash<QString, QString>` 临时保存演示验证码。
- 使用 `QMessageBox` 和 `QStatusBar` 给出错误提示和成功反馈。
- 服务端在线时调用 `ServerApiClient::requestSmsCode()` 和 `ServerApiClient::registerUser()`。
- 服务端离线时调用 `DatabaseManager::phoneExists()` 和 `DatabaseManager::registerUser()`。

对应代码：

- `MainWindow::handleSendRegisterCode()`
- `MainWindow::handleRegister()`
- `MainWindow::isPhoneValid()`
- `MainWindow::isPasswordValid()`
- `MainWindow::isNicknameValid()`
- `ServerApiClient::registerUser()`
- `DatabaseManager::registerUser()`

实现说明：

注册时先做客户端格式校验，再判断验证码是否正确。服务端可用时走 TCP 请求，注册成功后直接进入应用；服务端不可用时走 SQLite 本地演示数据。

### 3.2 需求 #9：登录功能

需求摘要：用户输入手机号和密码登录；支持 7 天免登录。

实现技术：

- 使用 `QRegularExpression` 校验手机号。
- 使用 `ServerApiClient::ping()` 判断服务端是否在线。
- 服务端模式下调用 `ServerApiClient::login()`，密码通过 `QCryptographicHash::Sha256` 做哈希后发送。
- 本地模式下调用 `DatabaseManager::login()`。
- 使用 `QSettings` 保存 `rememberUserId` 和 `rememberUntil`，实现 7 天免登录。
- 使用 `QStackedWidget` 从登录页切换到主应用页。

对应代码：

- `MainWindow::handleLogin()`
- `MainWindow::loadRememberedUser()`
- `MainWindow::enterApplication()`
- `ServerApiClient::login()`
- `ServerApiClient::hashedPassword()`
- `DatabaseManager::login()`

实现说明：

登录成功后会刷新个人资料、钱包余额、附近电站、流水记录，并检查是否存在未完成订单。服务端登录成功时保存 token，后续请求通过 token 标识当前用户。

### 3.3 需求 #10：找回密码功能

需求摘要：未登录状态下，通过手机号和验证码重置密码。

实现技术：

- 使用找回密码页中的 `QLineEdit` 输入手机号、验证码和新密码。
- 使用 `QHash` 保存找回密码验证码。
- 服务端模式下调用 `ServerApiClient::requestSmsCode()` 与 `ServerApiClient::resetPasswordByPhone()`。
- 本地模式下调用 `DatabaseManager::phoneExists()` 与 `DatabaseManager::resetPasswordByPhone()`。
- 使用 `QMessageBox` 提示验证码错误、密码违规、重置成功。

对应代码：

- `MainWindow::handleSendResetCode()`
- `MainWindow::handleResetPassword()`
- `ServerApiClient::resetPasswordByPhone()`
- `DatabaseManager::resetPasswordByPhone()`

实现说明：

验证码通过演示弹窗展示，便于课堂演示。新密码同样执行密码复杂度校验，成功后跳回登录页。

### 3.4 需求 #11：修改密码功能

需求摘要：登录后在个人资料页输入原密码、新密码并保存。

实现技术：

- 使用 `setPasswordEditorVisible()` 控制密码输入区展开/收起。
- 使用 `QLineEdit::Password` 类型输入原密码、新密码和确认密码。
- 使用 `QRegularExpression` 校验新密码复杂度。
- 服务端模式下调用 `ServerApiClient::changePassword()`。
- 本地模式下调用 `DatabaseManager::changePassword()`。

对应代码：

- `MainWindow::setPasswordEditorVisible()`
- `MainWindow::changePassword()`
- `ServerApiClient::changePassword()`
- `DatabaseManager::changePassword()`

实现说明：

默认只显示“修改密码”按钮，点击后才展开原密码、新密码、确认密码输入框，符合后续界面优化要求。

### 3.5 需求 #12：自定义头像

需求摘要：用户可上传 `.jpg` 图片作为头像，未上传时使用默认头像。

实现技术：

- 使用 `QFileDialog::getOpenFileName()` 选择本地头像文件。
- 使用 `QPixmap` 加载头像并显示在个人资料页与顶部用户区域。
- 使用 `resources.qrc` 引入默认头像 `resources/images/default_avatar.jpg`。
- 使用 QSS 设置头像边框和圆角。
- 本地模式下通过 `DatabaseManager::updateProfile()` 保存头像路径。
- 服务端模式下当前采用客户端侧资料缓存展示。

对应代码：

- `MainWindow::chooseAvatar()`
- `MainWindow::updateAvatarPreview()`
- `MainWindow::saveProfile()`
- `resources.qrc`
- `resources/images/default_avatar.jpg`

实现说明：

默认头像来自资源文件，程序打包后不依赖外部图片路径；用户选择本地头像后立即刷新界面预览。

### 3.6 需求 #13：自定义昵称

需求摘要：用户可自定义昵称，未设置时自动生成默认昵称。

实现技术：

- 使用 `QLineEdit` 编辑昵称。
- 使用 `QRegularExpression` 校验昵称格式。
- 注册时若昵称为空，自动生成 `user_手机号后四位`。
- 使用 `setProfileEditorVisible()` 控制资料编辑区展开/收起。
- 本地模式下通过 SQLite 保存，服务端模式下更新当前界面状态。

对应代码：

- `MainWindow::handleRegister()`
- `MainWindow::setProfileEditorVisible()`
- `MainWindow::saveProfile()`
- `DatabaseManager::updateProfile()`

实现说明：

个人资料页默认只展示资料，点击“修改资料”后才显示昵称输入框、选择头像、保存和取消按钮。

### 3.7 需求 #14：充值功能

需求摘要：登录后查看钱包余额，输入充值金额并提交，充值成功后余额实时刷新并写入流水。

实现技术：

- 使用 `QDoubleSpinBox` 输入充值金额。
- 使用 `QMessageBox` 显示充值成功反馈。
- 服务端模式下调用 `ServerApiClient::recharge()`。
- 本地模式下调用 `DatabaseManager::recharge()`，更新 `users.balance` 并插入 `transactions`。
- 使用 `refreshWallet()` 和 `queryTransactions()` 刷新余额与流水。

对应代码：

- `MainWindow::handleRecharge()`
- `MainWindow::refreshWallet()`
- `ServerApiClient::recharge()`
- `DatabaseManager::recharge()`

实现说明：

充值完成后顶部钱包摘要、钱包页余额和流水表格同步更新，避免用户看到旧数据。

### 3.8 需求 #15：流水记录查询功能

需求摘要：用户按时间范围查询充值、扣费等钱包流水。

实现技术：

- 使用 `QDateEdit` 选择开始日期和结束日期。
- 使用 `QTableWidget` 展示业务类型、金额、发生时间、说明。
- 使用 `adjustTransactionTableColumns()` 按 18% / 18% / 28% / 剩余宽度分配表格列宽，使流水表格铺满屏幕。
- 使用金额正负控制文字颜色，充值为绿色，扣费为红色。
- 服务端模式下调用 `ServerApiClient::transactions()`。
- 本地模式下调用 `DatabaseManager::transactions()`。

对应代码：

- `MainWindow::queryTransactions()`
- `MainWindow::adjustTransactionTableColumns()`
- `ServerApiClient::transactions()`
- `DatabaseManager::transactions()`

实现说明：

进入钱包页时会自动刷新流水；用户也可以手动点击查询按钮按时间范围筛选。

### 3.9 需求 #16：余额查询功能

需求摘要：登录后展示用户当前钱包余额，余额应与数据库一致。

实现技术：

- 使用顶部钱包卡片和钱包页余额标签双位置展示。
- 服务端模式下调用 `ServerApiClient::queryBalance()`。
- 本地模式下调用 `DatabaseManager::userById()` 读取最新用户信息。
- 使用 `refreshCurrentUser()` 统一刷新当前用户缓存。

对应代码：

- `MainWindow::refreshCurrentUser()`
- `MainWindow::refreshWallet()`
- `MainWindow::refreshProfile()`
- `ServerApiClient::queryBalance()`
- `DatabaseManager::userById()`

实现说明：

充值、充电结算、切换钱包页时都会刷新余额，保证页面数据和业务数据同步。

### 3.10 需求 #17：注销功能

需求摘要：用户登录后可注销，系统回到登录页，清理当前登录状态。

实现技术：

- 使用 `QSettings::remove()` 清理免登录信息。
- 服务端模式下调用 `ServerApiClient::logout()` 释放 token。
- 使用 `stopChargingThread()` 停止正在进行的充电线程。
- 使用 `QStackedWidget` 切回登录页。

对应代码：

- `MainWindow::handleLogout()`
- `MainWindow::stopChargingThread()`
- `ServerApiClient::logout()`

实现说明：

注销时同时清空当前用户、当前订单和服务端模式标记，防止下一个用户复用旧状态。

### 3.11 需求 #18：精确定位

需求摘要：登录后输入地址或选择区域，系统筛选附近充电站。

实现技术：

- 使用 `QComboBox` 选择区域。
- 使用 `QLineEdit` 输入地址关键字。
- 本地模式下通过 SQLite 的区域/地址条件查询站点。
- 服务端模式下通过 `ServerApiClient::stations()` 请求站点列表，内部携带演示经纬度和查询半径参数。
- 使用站点模型 `Station` 保存经纬度、距离、价格、推荐分等信息。

对应代码：

- `MainWindow::loadStations()`
- `ServerApiClient::stations()`
- `DatabaseManager::stations()`
- `models.h` 中的 `Station`

实现说明：

当前版本采用“区域筛选 + 地址关键字 + 演示经纬度”的定位方案，适合课堂演示附近站点筛选。如果严格要求真实地址解析，可继续接入腾讯地图 Web API 的地理编码接口。

### 3.12 需求 #19：充电站列表

需求摘要：加载并展示附近充电站列表，显示站名、电价、桩数量、空闲数量、距离等信息。

实现技术：

- 使用 `QListWidget` 展示站点列表。
- 使用 `QListWidgetItem::setData(Qt::UserRole, station.id)` 保存站点 ID。
- 使用 Qt 资源图标 `:/resources/icons/station.svg` 标识站点。
- 使用服务端或本地数据库返回的 `QVector<Station>` 作为页面数据源。

对应代码：

- `MainWindow::loadStations()`
- `ServerApiClient::stations()`
- `DatabaseManager::stations()`

实现说明：

站点列表按照查询条件刷新，选中第一条站点后自动加载其详情与电桩列表。

### 3.13 需求 #20：充电站详情

需求摘要：点击充电站后展示站点完整信息和站内电桩明细。

实现技术：

- 使用 `QListWidget::currentRowChanged` 信号触发站点详情刷新。
- 使用 `QLabel` 展示站点名称、地址、电价、距离、在线率、推荐说明。
- 使用 `QTableWidget` 展示电桩编号、类型、状态、功率、累计次数、累计时长。
- 服务端模式下调用 `ServerApiClient::pilesByStation()`。
- 本地模式下调用 `DatabaseManager::pilesByStation()`。

对应代码：

- `MainWindow::showStation()`
- `MainWindow::populatePiles()`
- `MainWindow::handlePileSelectionChanged()`
- `ServerApiClient::pilesByStation()`
- `DatabaseManager::pilesByStation()`

实现说明：

只有状态为“空闲”的电桩才允许开始充电，界面通过按钮启用/禁用给出明确反馈。

### 3.14 需求 #21：地图导航

需求摘要：选择目标充电站后，能够打开地图并进行导航。

实现技术：

- 使用站点的经纬度字段生成腾讯地图路线 URL。
- 使用 `QTextBrowser` 在界面中显示站点导航提示。
- 使用 `QDesktopServices::openUrl()` 调用系统浏览器打开腾讯地图路线规划。

对应代码：

- `MainWindow::tencentMapUrl()`
- `MainWindow::openNavigation()`
- `models.h` 中的 `Station::latitude`、`Station::longitude`

实现说明：

当前版本没有内嵌 `QWebEngineView`，而是通过系统浏览器打开腾讯地图 URL。这样减少了 Qt WebEngine 环境依赖，适合在虚拟机和课堂机房环境中稳定演示。

### 3.15 需求 #22：路线规划

需求摘要：用户选择驾车、步行、骑行、公交等方式后展示对应路线。

实现技术：

- 使用 `QComboBox` 保存路线方式。
- 将“驾车 / 步行 / 骑行 / 公共交通”映射为腾讯地图 URI 的 `drive / walk / bike / bus` 参数。
- 将用户输入的地址作为起点，将选中站点作为终点。
- 使用 URL 编码处理中文地址和站点名。

对应代码：

- `MainWindow::tencentMapUrl()`
- `MainWindow::openNavigation()`

实现说明：

路线规划由腾讯地图 Web 页面完成，用户端负责组织参数并拉起浏览器。

### 3.16 需求 #23：充电前检查功能

需求摘要：开始充电前，需要检查用户是否存在未完成订单。

实现技术：

- 使用 `std::optional<Order>` 表示可能存在或不存在的未完成订单。
- 服务端模式下调用 `ServerApiClient::unfinishedOrder()`。
- 本地模式下调用 `DatabaseManager::unfinishedOrder()`。
- 如果存在未完成订单，使用 `QMessageBox` 提示并切换到充电页处理。

对应代码：

- `MainWindow::prepareCharge()`
- `MainWindow::showUnfinishedOrder()`
- `ServerApiClient::unfinishedOrder()`
- `DatabaseManager::unfinishedOrder()`

实现说明：

该检查在登录后、进入充电页、点击开始充电前都会执行，避免用户重复创建充电订单。

### 3.17 需求 #24：充电服务

需求摘要：用户选择空闲电桩后开始充电，实时展示充电量、费用和进度，完成后生成订单并结算。

实现技术：

- 使用 `QTableWidget` 选择电桩。
- 使用 `QDoubleSpinBox` 设置目标充电量。
- 服务端模式下调用 `ServerApiClient::startCharge()` 创建服务端订单。
- 本地模式下调用 `DatabaseManager::createChargingOrder()` 创建本地订单。
- 使用 `QThread` 承载 `ChargingWorker`。
- `ChargingWorker` 内部使用 `QTimer` 周期性增加充电量并发射 `progressChanged()`。
- 主窗口通过 `onChargeProgress()` 刷新 `QProgressBar`、充电量标签和费用标签。
- 结束时服务端模式调用 `ServerApiClient::stopCharge()`，本地模式调用 `DatabaseManager::completeChargingOrder()` 和 `settleOrder()`。

对应代码：

- `MainWindow::prepareCharge()`
- `MainWindow::beginCharge()`
- `MainWindow::onChargeProgress()`
- `MainWindow::onChargeFinished()`
- `MainWindow::settleCurrentOrder()`
- `ChargingWorker::start()`
- `ChargingWorker::finish()`
- `ServerApiClient::startCharge()`
- `ServerApiClient::stopCharge()`
- `DatabaseManager::createChargingOrder()`
- `DatabaseManager::completeChargingOrder()`
- `DatabaseManager::settleOrder()`

实现说明：

充电过程放在工作线程中模拟，界面线程只接收信号刷新 UI，避免进度变化时窗口卡顿。

### 3.18 需求 #25：异常充电处理

需求摘要：充电过程中如果电桩故障，应终止当前充电，生成异常订单并提示用户处理。

实现技术：

- 使用“模拟故障”按钮触发异常流程。
- 使用 `QMetaObject::invokeMethod()` 以队列方式调用工作线程中的 `ChargingWorker::stopFault()`。
- `ChargingWorker::stopFault()` 发射 `finished(..., abnormal=true, ...)`。
- 本地模式下异常订单进入待结算状态；服务端模式下通过 `stopCharge()` 结束服务端订单。
- 使用 `QMessageBox` 和状态栏提示用户。

对应代码：

- `MainWindow::simulateFault()`
- `ChargingWorker::stopFault()`
- `MainWindow::onChargeFinished()`
- `MainWindow::settleCurrentOrder()`

实现说明：

异常处理与正常结束复用同一套订单结束信号，区别通过 `abnormal` 参数传递，减少重复逻辑。

## 4. 网络通信实现说明

用户端新增 `ServerApiClient` 和 `ClientProtocol` 两个类，将界面层与网络协议层分开。

### 4.1 协议封装

`ClientProtocol` 使用与服务端一致的二进制协议：

- `magic`：协议魔数 `0xE7CB1234`
- `requestId`：请求编号，用于匹配响应
- `bodyLength`：包体长度
- 包体内容：`quint16 cmd + QVariantMap params`

对应代码：

- `ClientProtocol::packMessage()`
- `ClientProtocol::tryParseMessage()`
- `ClientProtocol::writeHeader()`
- `ClientProtocol::readHeader()`

技术意义：

- 可以处理 TCP 粘包和半包问题。
- 可以通过 `requestId` 区分不同请求响应。
- 使用 `QDataStream::LittleEndian` 保持客户端和服务端字节序一致。

### 4.2 请求封装

`ServerApiClient::request()` 内部完成以下流程：

1. 创建 `QTcpSocket`。
2. 连接服务端 `127.0.0.1:8888`。
3. 使用 `ClientProtocol::packMessage()` 组包。
4. 调用 `write()` 和 `waitForBytesWritten()` 发送数据。
5. 循环读取响应数据。
6. 使用 `ClientProtocol::tryParseMessage()` 拆包。
7. 校验 `requestId`、响应命令号和业务状态码。
8. 返回 `QVariantMap` 给业务函数。

对应代码：

- `ServerApiClient::request()`
- `ServerApiClient::withToken()`
- `ServerApiClient::messageFromReply()`

技术意义：

主窗口不直接处理 socket 字节流，只调用注册、登录、充值、站点查询等业务方法，降低 UI 层复杂度。

## 5. 本地数据层实现说明

本地演示模式由 `DatabaseManager` 负责，使用 SQLite 存储和查询数据。

核心表：

- `users`：用户手机号、密码、昵称、头像、余额、状态
- `stations`：充电站名称、区域、地址、经纬度、电价、推荐分
- `piles`：电桩编号、所属站点、类型、功率、状态、累计次数、累计时长
- `transactions`：充值、扣费等流水
- `orders`：充电订单、状态、充电量、费用

对应代码：

- `DatabaseManager::open()`
- `DatabaseManager::createTables()`
- `DatabaseManager::seedDemoData()`
- `DatabaseManager::readUser()`
- `DatabaseManager::readStation()`
- `DatabaseManager::readPile()`
- `DatabaseManager::readOrder()`

技术意义：

即使服务端没有启动，用户端也能独立演示完整业务流程；服务端可用时则切换为真实 socket 通信。

## 6. 界面与交互实现说明

### 6.1 UI 文件

所有主要界面控件都在 `mainwindow.ui` 中设计，包括登录、注册、找回密码、主页面、钱包页、个人资料页和充电页。C++ 代码通过 `ui->控件名` 操作界面。

### 6.2 QSS 美化

`resources/styles/userclient.qss` 设置了：

- 全局字体与字号。
- 登录卡片、钱包卡片、个人资料卡片、顶部用户卡片样式。
- 按钮 hover、disabled、危险按钮样式。
- 表格、列表、进度条样式。
- 头像圆角边框。

### 6.3 资源文件

`resources.qrc` 管理：

- 应用图标：`app.svg`
- 功能图标：`station.svg`、`wallet.svg`、`profile.svg`、`charge.svg`
- 登录背景：`auth_background.png`
- 默认头像：`default_avatar.jpg`
- 样式表：`userclient.qss`

### 6.4 全屏显示

程序启动后在 `main.cpp` 中调用 `window.showMaximized()`，登录成功进入主应用时在 `MainWindow::enterApplication()` 中再次调用 `showMaximized()`，保证登录页和登录后界面默认最大化显示。

## 7. 事件机制实现说明

### 7.1 鼠标拖尾

`MouseTrailWidget` 使用事件过滤器和绘制事件实现鼠标拖尾：

- `eventFilter()` 捕获宿主控件及子控件上的鼠标移动、进入、离开、调整大小等事件。
- `recordMousePosition()` 记录鼠标轨迹点。
- `QTimer` 定期清理过期轨迹点。
- `paintEvent()` 使用 `QPainter` 绘制带透明度渐变的拖尾。

对应代码：

- `MouseTrailWidget::eventFilter()`
- `MouseTrailWidget::paintEvent()`
- `MouseTrailWidget::removeExpiredPoints()`
- `MouseTrailWidget::enableMouseTracking()`

技术意义：

该功能体现了 Qt 事件过滤器与自定义绘制的使用，也增强了界面科技感。

### 7.2 科技感背景

`TechPage` 通过重写 `paintEvent()` 绘制登录首页的蓝色科技线条，与背景图共同形成初始界面视觉效果。

对应代码：

- `TechPage::paintEvent()`

## 8. 验证方式

已编写一键验证脚本 `validate_all.ps1`，验证内容包括：

1. 同步用户端和服务端源码包到 Ubuntu 虚拟机。
2. 在 Ubuntu 中编译 `EVChargingServer`。
3. 在 Ubuntu 中编译 `ChargingUserClient`。
4. 启动或复用监听 `8888` 端口的服务端。
5. 编译并运行 `socket_smoke` 测试程序。
6. 验证用户端协议层可以完成登录、余额查询、站点查询、电桩查询。

验证输出：

```text
SOCKET_OK userId=1 nickname=张伟 balance=120.50 stations=3 firstStation=朝阳公园充电站 piles=10 records=0
VALIDATION_OK
```

测试账号：

- 手机号：`13800138000`
- 密码：`Demo@123`

## 9. 当前实现边界

以下内容在当前用户端中采用演示或替代实现：

- 精确定位：目前使用区域、地址关键字和演示经纬度完成站点筛选，未接入真实定位 SDK。
- 地图展示：目前通过腾讯地图 URI 拉起系统浏览器，未内嵌 `QWebEngineView`。
- 服务端个人资料更新：当前服务端协议未提供头像/昵称更新接口，服务端模式下资料修改主要用于当前客户端界面展示；本地模式下会写入 SQLite。

这些边界不影响用户端主体功能演示：注册、登录、找回密码、修改密码、钱包、流水、电站查询、电桩详情、充电、异常处理和服务端 socket 联通验证均已实现。
